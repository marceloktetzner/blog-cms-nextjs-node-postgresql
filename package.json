{
  "name": "identity-profiles",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "docker compose up -d && docker compose exec -T db bash -lc \"until pg_isready -U postgres; do sleep 1; done\" && npm --workspace apps/api run prisma:generate && npm --workspace apps/api run prisma:migrate && npm --workspace apps/api run dev",
    "start:api": "npm --workspace apps/api run start",
    "dev:web": "npm --workspace apps/web run dev",
    "build:web": "npm --workspace apps/web run build",
    "start:web": "npm --workspace apps/web run start"
  }
}
